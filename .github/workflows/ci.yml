name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore dotnet tools
        run: dotnet tool restore

      - name: Check formatting
        run: dotnet csharpier check .

      - name: Build (warnings are errors)
        run: dotnet build AgentWindows.slnx

      - name: Test with coverage
        run: dotnet test AgentWindows.slnx -- --coverlet --results-directory ./artifacts/coverage-raw

      - name: Coverage report
        run: |
          dotnet reportgenerator "-reports:artifacts/coverage-raw/coverage.cobertura.*.xml" "-targetdir:artifacts/coverage" "-reporttypes:Html;TextSummary"
          type artifacts\coverage\Summary.txt

      - uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: artifacts/coverage
